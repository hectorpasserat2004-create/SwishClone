// Module logique pure — aucune dépendance à AppKit ou macOS.
// En Phase 1, c'est ici que viendra la classification de gestes
// (fonctions pures : positions de doigts en entrée -> geste en sortie).

public enum SwishCloneCore {
    public static let version = "0.1.2"
}
